<% layout('layout') %>
<section class="jumbotron text-center">
    <div class="container">
     <h1><%= title %></h1>
     <h2><%= subtitle %></h2>
     <p class="lead text-muted"><%= description %></p>
     <img src=<%= img %>  width="800" />
    </div>
</section>